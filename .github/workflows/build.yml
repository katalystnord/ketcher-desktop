name: Build

on:
  push:
    branches: [master]
    tags: ['v*']
  pull_request:
    branches: [master]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: linux
          - os: windows-latest
            name: windows
          - os: macos-latest
            name: macos

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.name }})

    env:
      # ketcher-standalone bundles the Indigo modules, and rollup holds the lot in
      # memory. On the macOS runner (arm64, ~7 GB) Node's default heap ceiling is
      # not enough from Indigo v1.47.0-rc.1 onwards, and the build dies with
      # "FATAL ERROR: Ineffective mark-compacts near heap limit". Kept below the
      # runner's RAM so we do not trade V8's OOM for the kernel's. Harmless on the
      # roomier Linux and Windows runners.
      NODE_OPTIONS: --max-old-space-size=5120

    steps:
      - name: Enable long paths (Windows)
        if: runner.os == 'Windows'
        run: git config --system core.longpaths true

      - name: Checkout (with submodule)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Node 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Cache Ketcher node_modules
        uses: actions/cache@v4
        with:
          path: ketcher/node_modules
          key: ${{ runner.os }}-ketcher-${{ hashFiles('ketcher/package-lock.json') }}
          restore-keys: ${{ runner.os }}-ketcher-

      - name: Install desktop dependencies
        run: npm ci

      - name: Build Ketcher SPA
        run: bash scripts/build-ketcher.sh

      - name: Package
        run: npx electron-builder --publish never

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ketcher-desktop-${{ matrix.name }}
          path: |
            dist-electron/*.AppImage
            dist-electron/*.deb
            dist-electron/*.exe
            dist-electron/*.dmg
          if-no-files-found: warn

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**
          draft: true
          generate_release_notes: true
